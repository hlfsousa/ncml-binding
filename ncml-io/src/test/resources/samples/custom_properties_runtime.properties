customglobalattribute=title
# all possible values of mapped properties must be added, because the key is determined by the actual full name. I recon
# this is bad (very bad), but it is a necessary evil for now
g01/items=components
g02/items=components

# this would be better (but it could slow things down)
regex.g\d+/items=components

this_group_name_is_not_good_for_a_property=better_group_name